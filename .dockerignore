# 构建产物：多阶段构建在容器内重新编译，本机 target 不需要进入构建上下文
target/

# 版本控制与文档：与镜像无关，带进去只会拖慢构建（构建上下文要整包传给 Docker daemon）
.git/
docs/
*.md

# 本地构建包装器：构建阶段用的是 maven 镜像自带的 mvn，不需要这两个
mvnw
mvnw.cmd

# IDE 配置
.idea/
*.iml
.vscode/

# 本地环境变量：绝不能进镜像
.env
